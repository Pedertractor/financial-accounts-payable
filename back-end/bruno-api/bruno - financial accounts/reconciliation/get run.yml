info:
  name: get run
  type: http
  seq: 2

http:
  method: GET
  url: "{{HOST}}/reconciliation/runs/{{RUN_ID}}"
  auth:
    type: bearer
    token: "{{TOKEN}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar 200", function () {
          expect(res.status).to.equal(200);
        });
        test("deve conter run", function () {
          expect(res.body).to.have.property("run");
        });

docs: |-
  Detalhe da execução. Defina `RUN_ID` no environment (ou rode **create run** antes).

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
