info:
  name: first password
  type: http
  seq: 6

http:
  method: POST
  url: "{{HOST}}/users/first-password"
  body:
    type: json
    data: |-
      {
        "userId": "{{USER_ID}}",
        "newPassword": "pedro10"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
