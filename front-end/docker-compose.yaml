services:
  frontend-finance-accounts-payble:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3134:80'
    networks:
      - finance-accounts-payble-network

networks:
  finance-accounts-payble-network:
